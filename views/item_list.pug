extends layout

block content
    h1 Items:

    ul
        each item in item_list
            li 
                a(href=item.url) !{item.name}
            if item.filename
                img.img-small(src=item.imageurl alt='item image')
            hr
        else
            li There are no items